/** @type {import('next').NextConfig} */
const nextConfig = {
  // Firebase modular SDK e libs client-only (PDF.js, Tesseract.js) rodam
  // dentro de client components; nada aqui precisa de config especial ainda.
};

export default nextConfig;
